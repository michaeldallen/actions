name :  shell commands

on: [push]

jobs: 
    run-shell-command: 
        runs-on: ubuntu-24.04
        steps: 
          - name: echo a string
            run: echo "hello A"
          - name: echo multiline
            run: |
                node -v
                npm -v
                
                
